services:
  express-app:
    build: .
    container_name: coll-website

    ports:
      - "8000:8000"

    volumes:
      - .:/app
      - /app/node_modules

    restart: unless-stopped

    command: npm run dev